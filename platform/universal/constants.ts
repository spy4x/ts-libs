/** Default trailing-edge debounce delay, in milliseconds. */
export const DEFAULT_DEBOUNCE_DELAY = 300

/** Concurrency used by `mapConcurrent` when a caller has no stronger opinion. */
export const DEFAULT_CONCURRENCY = 4

/** Default window for the throttled JSON saver, in milliseconds. */
export const DEFAULT_FLUSH_INTERVAL_MS = 2000
