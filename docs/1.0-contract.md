# 1.0 contract — first draft

Generated by `deno task contract` from the code on the branch it runs on. Do not edit by hand;
change the code or the entry list in `infra/scripts/contract.ts`, then run the task again.

This is the frame for condition 2 in #77 ("The interfaces are frozen at 1.0"): for each entry
point the template will import, its exported names and their signatures. After 1.0, a change to
anything listed here is additive or waits for 2.0. JSDoc is left out; the code holds it.

Each signature is `deno doc`'s own text rendering. That rendering leaves out a method's own type
parameters (`register<T extends Command<unknown, unknown>>(…)` prints as `register(…)`) and
prints a mapped type without its braces. It also leaves out a parameter's default value, so a
parameter with a default (`init: RequestInit = {}`) reads as required here although a caller may
omit it. Where a line here and the source file named under its heading differ, the source file
is the contract.

## `@spy4x/validation`

Capability: schema validation. Source: `validation/mod.ts`.

```text
const FORM_FIELD: "_form"

enum ValidationType
  SCHEMA = "SCHEMA"

function firstIssueMessage(error: ValidationError): string | null

function isArkErrors(value: unknown): value is ArkErrors

function isValid<M extends object>(vl: ValidationModel<M>): boolean

function sameValidation<M extends object>(a: ValidationModel<M>, b: ValidationModel<M>): boolean

function schemaIssues<S extends Type>(schema: S, value: unknown): Record<string, FieldValidation>

function setFieldIssue<M extends object>(vl: ValidationModel<M>, field: keyof M & string, type: string, message: string | undefined, payload?: number): ValidationModel<M>

function toValidationError(issues: ArkErrors): ValidationError

function validate<T extends Type>(schema: T, value: unknown): ValidationResult<T>

function validateSchema<S extends Type, M extends object>(schema: S, value: M, vl: ValidationModel<M>): ValidationModel<M>

interface FieldIssue
  message: string
  payload?: number

interface ValidationError
  description: string
  details: ArkErrors

type FieldValidation = Record<string, FieldIssue | undefined>

type SchemaInput<T extends Type> = T["inferIn"]

type SchemaOutput<T extends Type> = T["infer"]

type ValidationModel<M extends object> = [K in keyof M]?: FieldValidation & Record<string, FieldValidation | undefined>

type ValidationResult<T extends Type> = { error: ValidationError; data: null; } | { error: null; data: SchemaOutput<T>; }
```

## `@spy4x/platform/cqrs`

Capability: command, query and event bus. Source: `platform/cqrs/mod.ts`.

```text
class CommandBus
  register(commandClass: CommandConstructor<T>, handler: CommandHandler<T>): void
  async execute(command: T): Promise<CommandResult<T>>
  getRegisteredCommands(): string[]

class EventBus
  constructor(private onListenerError: EventBusErrorHandler)
  on(eventClass: EventConstructor<T>, callback: (event: T) => void | Promise<void>): () => void
  once(eventClass: EventConstructor<T>, callback: (event: T) => void | Promise<void>): () => void
  emit(event: T): void

class QueryBus
  register(queryClass: QueryConstructor<T>, handler: QueryHandler<T>): void
  async execute(query: T): Promise<QueryResult<T>>
  getRegisteredQueries(): string[]

interface Command<TPayload, TResult>
  data: TPayload
  readonly __resultType?: TResult

interface Event<TPayload>
  data?: TPayload

interface Query<TPayload, TResult>
  data: TPayload
  readonly __resultType?: TResult

type CommandConstructor<T extends Command<unknown, unknown>> = new (...args: any[]) => T

type CommandHandler<T extends Command<unknown, unknown>> = (command: T) => Promise<CommandResult<T>>

type CommandResult<T extends Command<unknown, unknown>> = T extends Command<unknown, infer R> ? R : never

type EventBusErrorHandler = (eventClass: EventConstructor<Event<unknown>>, error: unknown) => void

type EventConstructor<T extends Event<unknown>> = new (...args: any[]) => T

type QueryConstructor<T extends Query<unknown, unknown>> = new (...args: any[]) => T

type QueryHandler<T extends Query<unknown, unknown>> = (query: T) => Promise<QueryResult<T>>

type QueryResult<T extends Query<unknown, unknown>> = T extends Query<unknown, infer R> ? R : never
```

## `@spy4x/platform/cache`

Capability: cache with `wrap()`. Source: `platform/cache/mod.ts`.

```text
class CacheService implements ICacheService
  constructor(private storage: ICacheStorage, private options: CacheServiceOptions)
  async get(key: string): Promise<T | null>
  async set(key: string, value: T, ttlSec: number): Promise<void>
  async delete(key: string): Promise<void>
  async wrap(key: string, fn: () => Promise<T>, ttlSec: number, options: CacheWrapOptions): Promise<T>
  async reset(): Promise<void>

function buildMethods<T, K extends string | number = number>(cacheService: ICacheService, prefix: string, ttlSec: number, schema?: Type): PublicAPICacheModel<T, K>

function reviveIsoDatesEndingInAt(key: string, value: unknown): unknown

interface CacheServiceOptions
  reviver?: (key: string, value: unknown) => unknown

interface CacheWrapOptions
  shouldSaveFalsy?: boolean

interface ICacheService
  get(key: string): Promise<T | null>
  set(key: string, value: T, ttlSec: number): Promise<void>
  delete(key: string): Promise<void>
  wrap(key: string, fn: () => Promise<T>, ttlSec: number, options?: CacheWrapOptions): Promise<T>
  reset(): Promise<void>

interface ICacheStorage
  get(key: string): Promise<string | null>
  set(key: string, value: string, ttlSec: number): Promise<void>
  del(key: string): Promise<void>
  reset(): Promise<void>

interface PublicAPICacheModel<T, K extends string | number = number>
  readonly ttl: number
  key(id: K): string
  get(id: K): Promise<T | null>
  set(id: K, item: T): Promise<void>
  delete(id: K): Promise<void>
  wrap(id: K, fn: () => Promise<T>): Promise<T>
  wrapMany(listPrefix: string, fn: () => Promise<T[]>): Promise<T[]>
```

## `@spy4x/platform/tokens`

Capability: random tokens. Source: `platform/tokens.ts`.

```text
async function constantTimeEquals(a: string, b: string): Promise<boolean>

async function newOpaqueToken(secret: string): Promise<{ raw: string; hash: string; }>

async function sha256Hex(input: string): Promise<string>

async function verifyOpaqueToken(raw: string, hash: string, secret: string): Promise<boolean>

class TokenError extends Error
  constructor(code: TokenErrorCode, message: string, options?: { cause?: unknown; })
  readonly code: TokenErrorCode

const DEFAULT_TOKEN_BYTES: 16

const MIN_SECRET_LENGTH: 32

const ULID_LENGTH: 26

enum TokenErrorCode
  InvalidSecret = 1
  InvalidByteCount = 2

function monotonicUlid(seedTime: number): string

function randomBase64Url(bytes: number): string
```

## `@spy4x/platform/rate-limit`

Capability: rate limiting. Source: `platform/rate-limit/mod.ts`.

```text
class MemoryRateLimiter
  constructor(options: MemoryRateLimiterOptions)
  get size(): number
  check(key: string, now: number): RateLimitDecision
  reset(key: string): void
  clear(): void
  sweep(now: number): number

class RateLimitStoreOverKv implements RateLimitStore
  constructor(options: KvStoreOptions)
  keyFor(key: string): string
  async read(key: string, now: number): Promise<number[] | undefined>
  async write(key: string, events: number[], now: number, ttlMs: number): Promise<void>
  async delete(key: string): Promise<void>

class StoreRateLimiter
  constructor(store: RateLimitStore, options: RateLimitOptions)
  async check(key: string, now: number): Promise<RateLimitDecision>
  async reset(key: string): Promise<void>

const DEFAULT_IDLE_MS: number

const RATE_LIMIT_HEADERS: RateLimitHeaderNames

const REJECTION_STATUSES: ReadonlySet<number>

const SWEEP_EVERY_CHECKS: 512

const UNKNOWN_CLIENT_IP: "0.0.0.0"

enum RateLimitKind
  Ip = 1
  User = 2

function clientIp(req: Request, remoteAddr?: string, trustedProxy: boolean): string

function createKvStore(options: KvStoreOptions): RateLimitStore

function createMemoryRateLimiter(options: MemoryRateLimiterOptions): MemoryRateLimiter

function createRateLimitMiddleware<E extends Env = Record<string, never>>(rateLimiter: RateLimiter, options: RateLimitMiddlewareOptions<E>): MiddlewareHandler<E>

function createStoreLimiter(store: RateLimitStore, options: RateLimitOptions): StoreRateLimiter

function decisionHeaders(decision: RateLimitDecision, headers: RateLimitHeaderNames): Record<string, string>

function denoKvBackend(kv: DenoKvLike, namespace: string): RateLimitKv

function humanRetry(ms: number): string

function rateLimitKey(kind: RateLimitKind, id: string, prefix: string): string

function systemClock()

function userThenIp<E extends Env = Record<string, never>>(userId: (req: Request, context: RateLimitContext<E>) => string | undefined | Promise<string | undefined>, options: { trustedProxy?: boolean; }): KeyResolver<E>

interface DenoKvLike
  get(key: readonly string[]): Promise<{ value: unknown; }>
  set(key: readonly string[], value: unknown, options?: { expireIn?: number; }): Promise<unknown>
  delete(key: readonly string[]): Promise<void>

interface KvStoreOptions
  backend: RateLimitKv
  keyPrefix?: string
  clock?: Clock

interface MemoryRateLimiterOptions extends RateLimitOptions
  onSweep?: (removed: number) => void

interface RateLimitContext<E extends Env = Record<string, never>>
  req: Request
  env?: E
  get?: (key: string) => unknown

interface RateLimitDecision
  allowed: boolean
  remaining: number
  retryAfterMs: number
  resetAfterMs: number
  limit: number

interface RateLimiter
  check(key: string, now?: number): RateLimitDecision | Promise<RateLimitDecision>
  reset(key: string, now?: number): void | Promise<void>

interface RateLimitErrorBody
  error: string

interface RateLimitHeaderNames
  limit: string
  remaining: string
  reset: string
  retryAfter: string

interface RateLimitKv
  get(key: string): Promise<unknown>
  set(key: string, value: unknown, options?: { expireIn?: number; }): Promise<void>
  delete(key: string): Promise<void>

interface RateLimitKvEntry
  events: number[]
  expiresAt: number

interface RateLimitMiddlewareOptions<E extends Env = Record<string, never>>
  keyResolver: KeyResolver<E>
  remoteAddr: RemoteAddrResolver<E>
  errorMessage?: string
  status?: RejectionStatus
  keyPrefix?: string
  headers?: Partial<RateLimitHeaderNames>

interface RateLimitOptions
  windowMs: number
  limit: number
  clock?: Clock
  idleMs?: number

interface RateLimitStore
  read(key: string, now?: number): Promise<number[] | undefined>
  write(key: string, events: number[], now: number, ttlMs: number): Promise<void>
  delete(key: string): Promise<void>

type Clock = () => number

type KeyResolver<E extends Env = Record<string, never>> = (req: Request, context: RateLimitContext<E>) => string | Promise<string>

type RateLimitHonoContext<E extends Env = Record<string, never>> = Context<E>

type RateLimitMiddleware<E extends Env = Record<string, never>> = MiddlewareHandler<E>

type RejectionStatus = 400 | 401 | 403 | 404 | 408 | 409 | 410 | 418 | 422 | 425 | 429 | 451 | 500 | 502 | 503 | 504

type RemoteAddrResolver<E extends Env = Record<string, never>> = (context: RateLimitContext<E>) => string | undefined
```

## `@spy4x/platform/rate-limit/hono`

Capability: rate limiting. Source: `platform/rate-limit/hono.ts`.

```text
const RATE_LIMIT_HEADERS: RateLimitHeaderNames

const REJECTION_STATUSES: ReadonlySet<number>

function createRateLimitMiddleware<E extends Env = Record<string, never>>(rateLimiter: RateLimiter, options: RateLimitMiddlewareOptions<E>): MiddlewareHandler<E>

function decisionHeaders(decision: RateLimitDecision, headers: RateLimitHeaderNames): Record<string, string>

function userThenIp<E extends Env = Record<string, never>>(userId: (req: Request, context: RateLimitContext<E>) => string | undefined | Promise<string | undefined>, options: { trustedProxy?: boolean; }): KeyResolver<E>

interface RateLimitContext<E extends Env = Record<string, never>>
  req: Request
  env?: E
  get?: (key: string) => unknown

interface RateLimitErrorBody
  error: string

interface RateLimitHeaderNames
  limit: string
  remaining: string
  reset: string
  retryAfter: string

interface RateLimitMiddlewareOptions<E extends Env = Record<string, never>>
  keyResolver: KeyResolver<E>
  remoteAddr: RemoteAddrResolver<E>
  errorMessage?: string
  status?: RejectionStatus
  keyPrefix?: string
  headers?: Partial<RateLimitHeaderNames>

type KeyResolver<E extends Env = Record<string, never>> = (req: Request, context: RateLimitContext<E>) => string | Promise<string>

type RateLimitHonoContext<E extends Env = Record<string, never>> = Context<E>

type RateLimitMiddleware<E extends Env = Record<string, never>> = MiddlewareHandler<E>

type RejectionStatus = 400 | 401 | 403 | 404 | 408 | 409 | 410 | 418 | 422 | 425 | 429 | 451 | 500 | 502 | 503 | 504

type RemoteAddrResolver<E extends Env = Record<string, never>> = (context: RateLimitContext<E>) => string | undefined
```

## `@spy4x/platform/api`

Capability: shared API and model types. Source: `platform/api/mod.ts`.

```text
async function apiFetch<T>(path: string, init: RequestInit): Promise<ApiResult<T>>

interface ApiError
  status: number
  message: string

type ApiResult<T> = { ok: true; status: number; data: T; } | { ok: false; status: number; error: ApiError; }
```

## `@spy4x/platform/request-info`

Capability: shared API and model types. Source: `platform/request-info/mod.ts`.

```text
function requestInfoFromContext<E extends Env = Record<string, never>>(c: Context<E>, options: RequestInfoOptions): RequestInfo

interface RequestInfo
  requestId?: string
  ip?: string
  userAgent?: string

interface RequestInfoOptions
  trustedProxy?: boolean
  remoteAddr?: string
```

## `@spy4x/platform/model`

Capability: shared API and model types. Source: `platform/model/mod.ts`.

```text
const BaseModelSchema: Type<{ id: number; createdAt: Date | ((In: string) => Out<Date>); updatedAt: Date | ((In: string) => Out<Date>); deletedAt: Default<Date | null, null> | ((In: Default<string, null>) => Out<Date>); }>

const DateNullableSchema: [Type<Date | ((In: string) => Out<Date>) | null>, "=", null]

const dateSchema: Type<Date | ((In: string) => Out<Date>)>

const ImmutableBaseModelSchema: Type<{ id: number; createdAt: Date | ((In: string) => Out<Date>); }>

const pushNotificationMessageSchema: Type<{ title: string; body: Default<string | null, null>; url: Default<string | null, null>; }>

const pushSubscribeRequestSchema: Type<{ deviceId: string; subscription: { endpoint: string; expirationTime: Default<number | null, null>; keys: { auth: string; p256dh: string; }; }; }>

const pushSubscriptionKeysSchema: Type<{ auth: string; p256dh: string; }>

const pushSubscriptionSchema: Type<{ endpoint: string; expirationTime: Default<number | null, null>; keys: { auth: string; p256dh: string; }; }>

const pushUnsubscribeRequestSchema: Type<{ deviceId: string; }>

const UndeletableBaseModelSchema: Type<{ id: number; createdAt: Date | ((In: string) => Out<Date>); updatedAt: Date | ((In: string) => Out<Date>); }>

type BaseModel = typeof BaseModelSchema.infer

type DateType = typeof dateSchema.infer

type PushNotificationMessage = typeof pushNotificationMessageSchema.infer

type PushSubscribeRequest = typeof pushSubscribeRequestSchema.infer

type PushSubscriptionData = PushSubscriptionJson

type PushSubscriptionJson = typeof pushSubscriptionSchema.infer

type PushSubscriptionKeys = typeof pushSubscriptionKeysSchema.infer

type PushUnsubscribeRequest = typeof pushUnsubscribeRequestSchema.infer
```

## `@spy4x/server/kv`

Capability: Redis key-value store. Source: `server/kv/mod.ts`.

```text
class RedisKvStore
  private constructor(private readonly connection: Deno.Conn, private readonly client: RedisClient, private readonly keyPrefix: string, connectionError: ConnectionErrorHolder)
  static async connect(hostname: string, port: number, keyPrefix: string): Promise<RedisKvStore>
  async get(key: string): Promise<string | null>
  async set(key: string, value: string, ttlSec: number): Promise<void>
  async del(key: string): Promise<void>
  async clientId(): Promise<number>
  async reset(): Promise<void>
  close(): void

class RedisKvStoreClosedError extends Error
  constructor()

class RedisKvStoreConnectionError extends Error
  constructor(cause: unknown)
```

## `@spy4x/server/outbox`

Capability: transactional outbox. Source: `server/outbox/mod.ts`.

```text
class LoggingOutboxPublisher implements OutboxPublisher
  publish(event: OutboxEvent): Promise<void>

class OutboxProcessor
  constructor(private readonly repository: OutboxRepository, private readonly publisher: OutboxPublisher, options: OutboxProcessorOptions)
  async drainOnce(): Promise<DrainResult>
  async run(signal: AbortSignal, idleDelayMs: number): Promise<void>

class PostgresOutboxRepository implements OutboxRepository
  constructor(private readonly sql: Sql)
  async claimBatch(limit: number, maxAttempts: number, leaseSeconds: number): Promise<OutboxEvent[]>
  async markProcessed(id: string): Promise<void>
  async scheduleRetry(id: string, delaySeconds: number, errorCode: string): Promise<void>

function errorCodeOf(error: unknown): string

function retryDelayMs(attemptCount: number, baseMs: number, maxMs: number): number

interface DrainResult
  claimed: number
  published: number
  failed: number

interface OutboxEvent
  id: string
  eventKind: string
  aggregateType: string
  aggregateId: string
  aggregateVersion: string
  attemptCount: number

interface OutboxProcessorOptions
  batchSize?: number
  maxAttempts?: number
  baseRetryDelayMs?: number
  maxRetryDelayMs?: number
  leaseSeconds?: number

interface OutboxPublisher
  publish(event: OutboxEvent): Promise<void>

interface OutboxRepository
  claimBatch(limit: number, maxAttempts: number, leaseSeconds: number): Promise<OutboxEvent[]>
  markProcessed(id: string): Promise<void>
  scheduleRetry(id: string, delaySeconds: number, errorCode: string): Promise<void>
```

## `@spy4x/server/sign-in`

Capability: session, cookie, TOTP, password hash, auth guards. Source: `server/sign-in/mod.ts`.

```text
class SessionCookie
  constructor(options: SessionCookieOptions)
  async read(c: Context): Promise<string | null>
  async set(c: Context, session: { userId: number; expiresAt: Date; }, value: string): Promise<void>
  clear(c: Context): void

class SessionManager<S extends SessionRecord = SessionRecord>
  constructor(options: SessionManagerOptions<S>)
  async create(fields: NewSessionFields<S>): Promise<CreatedSession<S>>
  async validate(cookieValue: string): Promise<ValidSession<S> | null>
  async signOut(cookieValue: string): Promise<boolean>
  async signOutUser(userId: number, options: { except?: number; }): Promise<void>
  async completeSecondFactor(sessionId: number): Promise<boolean>
  async expireStale(): Promise<void>

const DEFAULT_PASSWORD_ITERATIONS: 600000

const MAX_PASSWORD_BYTES: 1024

const MAX_SESSION_MINUTES: number

const NOT_AUTHENTICATED: "Not authenticated"

const NOT_AUTHORIZED: "Not authorized"

const SECOND_FACTOR_REQUIRED: "Need to pass 2FA"

const SESSION_COOKIE_NAME: "sessionIdToken"

const TOTP_DIGITS: 6

const TOTP_PERIOD_SECONDS: 30

const TOTP_WINDOW: 1

const USER_ID_COOKIE_NAME: "user_id"

enum SecondFactorStatus
  NotRequired = 1
  Pending = 2
  Completed = 3

enum SessionStatus
  Active = 1
  Expired = 2
  SignedOut = 3

function createAuth<S extends SessionRecord, U>(options: AuthOptions<S, U>): Auth<S, U>

function createPasswordHasher(options: PasswordHasherOptions): PasswordHasher

function generateTotpSecret(): string

function totpEnrolment(secret: string, account: TotpAccount): TotpEnrolment

function verifyTotp(secret: string, code: string, options: TotpVerifyOptions): number | null

interface Auth<S extends SessionRecord, U>
  parseAuth: MiddlewareHandler<AuthEnv<S, U>>
  isAuthenticated1FA: MiddlewareHandler<AuthEnv<S, U>>
  isAuthenticated2FA: MiddlewareHandler<AuthEnv<S, U>>
  isAuthorized(check: (auth: AuthState<S, U>) => boolean | Promise<boolean>): MiddlewareHandler<AuthEnv<S, U>>
  startSession(c: Context, fields: NewSessionFields<S>): Promise<S>
  endSession(c: Context<AuthEnv<S, U>>): Promise<void>

interface AuthEnv<S extends SessionRecord, U>
  Variables: { auth: AuthState<S, U> | null; }

interface AuthOptions<S extends SessionRecord, U>
  sessions: SessionManager<S>
  cookie: SessionCookie
  loadUser(userId: number): Promise<U | null>
  hasSecondFactor(user: U): boolean

interface AuthState<S extends SessionRecord, U>
  session: S
  user: U

interface CreatedSession<S extends SessionRecord = SessionRecord>
  session: S
  cookieValue: string

interface PasswordCheck
  valid: boolean
  needsRehash: boolean

interface PasswordHasher
  hash(password: string): Promise<string>
  verify(password: string, stored: string): Promise<PasswordCheck>

interface PasswordHasherOptions
  pepper: string
  iterations?: number

interface SessionCookieOptions
  secret: string
  secure?: boolean
  name?: string
  userIdName?: string
  clock?: Clock

interface SessionManagerOptions<S extends SessionRecord = SessionRecord>
  store: SessionStore<S>
  pepper: string
  durationMinutes: number
  clock?: Clock

interface SessionRecord
  id: number
  userId: number
  tokenHash: string
  status: SessionStatus
  secondFactor: SecondFactorStatus
  expiresAt: Date

interface SessionStore<S extends SessionRecord = SessionRecord>
  create(session: Omit<S, "id">): Promise<S>
  findById(id: number): Promise<S | null>
  extend(id: number, expiresAt: Date): Promise<boolean>
  completeSecondFactor(id: number): Promise<boolean>
  signOut(id: number): Promise<void>
  signOutUser(userId: number, exceptId: number | null): Promise<void>
  expire(now: Date): Promise<void>

interface TotpAccount
  issuer: string
  label: string

interface TotpEnrolment
  uri: string
  qrCodeSvg: string

interface TotpVerifyOptions
  lastAcceptedStep: number | null
  clock?: Clock

interface ValidSession<S extends SessionRecord = SessionRecord>
  session: S
  extended: boolean

type NewSessionFields<S extends SessionRecord = SessionRecord> = Omit<S, "id" | "tokenHash" | "status" | "expiresAt">
```

## `@spy4x/server/auth`

Capability: password sign-up and sign-in, mail codes, OAuth (the account model and store). Source: `server/auth/mod.ts`.

```text
class AuthConflictError extends Error
  constructor(reason: AuthConflictReason, message?: string)
  readonly reason: AuthConflictReason

const AUTH_STORE_METHODS: readonly string[]

const MAX_EMAIL_LENGTH: 254

enum ChallengeOutcome
  Matched = 1
  WrongGuess = 2
  LockedOut = 3
  Missing = 4

function normalizeEmail(raw: unknown): string | null

interface AttemptChallengeInput
  purpose: string
  subject: string
  secretHash: string
  maxAttempts: number
  now: Date

interface AuthKey
  id: number
  userId: number
  method: string
  subject: string
  email: string | null
  secret: string | null
  provenAt: Date | null
  createdAt: Date
  updatedAt: Date

interface AuthSessionRecord extends SessionRecord
  keyId: number

interface AuthStore
  findUser(id: number): Promise<AuthUser | null>
  createUserWithKey(key: NewAuthKey): Promise<{ user: AuthUser; key: AuthKey; }>
  addKey(userId: number, key: NewAuthKey): Promise<AuthKey>
  findKey(method: string, subject: string): Promise<AuthKey | null>
  findKeyById(id: number): Promise<AuthKey | null>
  listKeys(userId: number): Promise<AuthKey[]>
  findUserIdByProvenEmail(email: string): Promise<number | null>
  proveKey(keyId: number, now: Date): Promise<AuthKey>
  updateKeySecret(keyId: number, secret: string): Promise<boolean>
  deleteKey(userId: number, keyId: number): Promise<boolean>
  issueChallenge(input: IssueChallengeInput): Promise<void>
  attemptChallenge(input: AttemptChallengeInput): Promise<ChallengeOutcome>

interface AuthUser
  id: number
  createdAt: Date
  deletedAt: Date | null

interface IssueChallengeInput
  purpose: string
  subject: string
  secretHash: string
  expiresAt: Date
  now: Date

interface NewAuthKey
  method: string
  subject: string
  email: string | null
  secret: string | null
  provenAt: Date | null

interface ProviderDeps
  store: AuthStore
  sessions: SessionManager<AuthSessionRecord>
  clock?: Clock
  secondFactorFor?(user: AuthUser): Promise<SecondFactorStatus>

interface SignInResult
  user: AuthUser
  key: AuthKey
  session: CreatedSession<AuthSessionRecord>

type AuthConflictReason = "key-exists" | "email-owned"
```

## `@spy4x/server/auth/postgres`

Capability: password sign-up and sign-in, mail codes, OAuth (the account model and store). Source: `server/auth/postgres.ts`.

```text
const AUTH_POSTGRES_SCHEMA: string

function createPostgresAuthStore(sql: Sql): AuthStore

function createPostgresSessionStore(sql: Sql): SessionStore<AuthSessionRecord>
```

## `@spy4x/server/auth/password`

Capability: password sign-up and sign-in, mail codes, OAuth (password). Source: `server/auth/password.ts`.

```text
class PasswordSignInError extends Error
  constructor(reason: PasswordSignInFailure, message?: string)
  readonly reason: PasswordSignInFailure

const DEFAULT_MAX_RESET_ATTEMPTS: 5

const DEFAULT_MIN_PASSWORD_LENGTH: 8

const DEFAULT_RESET_TTL_MINUTES: 30

const PASSWORD_METHOD: "password"

const PASSWORD_RESET_PURPOSE: "password-reset"

function createPasswordSignIn(options: PasswordSignInOptions): PasswordSignIn

interface ChangePasswordInput
  userId: number
  currentPassword: string
  newPassword: string

interface CompleteResetInput
  email: string
  code: string
  newPassword: string

interface IssuedReset
  email: string
  code: string
  expiresAt: Date

interface PasswordCredentials
  email: string
  password: string

interface PasswordSignIn
  signUp(input: PasswordCredentials): Promise<SignInResult>
  signIn(input: PasswordCredentials): Promise<SignInResult>
  changePassword(input: ChangePasswordInput): Promise<SignInResult>
  requestReset(input: { email: string; }): Promise<IssuedReset>
  completeReset(input: CompleteResetInput): Promise<SignInResult>

interface PasswordSignInOptions extends ProviderDeps
  hasher: PasswordHasher
  minPasswordLength?: number
  resetTtlMinutes?: number
  maxResetAttempts?: number

type PasswordSignInFailure = "invalid-email" | "invalid-password" | "email-taken" | "invalid-credentials" | "invalid-code" | "locked-out" | "no-account" | "conflict"
```

## `@spy4x/server/auth/email-code`

Capability: password sign-up and sign-in, mail codes, OAuth (mail codes). Source: `server/auth/email-code.ts`.

```text
class EmailCodeError extends Error
  constructor(reason: EmailCodeErrorReason)
  readonly reason: EmailCodeErrorReason

const DEFAULT_CODE_MAX_ATTEMPTS: 5

const DEFAULT_CODE_TTL_MINUTES: 10

const EMAIL_CODE_BYTES: 6

const EMAIL_CODE_METHOD: "email-code"

const EMAIL_CODE_PURPOSE: "email-code"

function createEmailCodeSignIn(deps: EmailCodeSignInDeps): EmailCodeSignIn

interface EmailCodeSignIn
  requestCode(email: string): Promise<void>
  verifyCode(email: string, code: string): Promise<SignInResult>

interface EmailCodeSignInDeps extends ProviderDeps
  codeTtlMinutes?: number
  maxAttempts?: number
  sendCode(email: string, code: string): Promise<void>

type EmailCodeErrorReason = "invalid-email" | "wrong-code" | "locked-out" | "no-code" | "account-deleted"
```

## `@spy4x/server/auth/oauth`

Capability: password sign-up and sign-in, mail codes, OAuth (OAuth). Source: `server/auth/oauth.ts`.

```text
async function pkceChallenge(verifier: string): Promise<string>

class OAuthSignInError extends Error
  constructor(reason: OAuthFailure, options?: { cause?: unknown; })
  readonly reason: OAuthFailure

const MAX_PENDING_OAUTH_FLOWS: 10000

enum OAuthOutcome
  SignedIn = 1
  SignedUp = 2
  Linked = 3

function createOAuthSignIn(options: OAuthSignInOptions): OAuthSignIn

interface OAuthAuthorization
  url: URL
  state: string
  expiresAt: Date

interface OAuthCallbackInput
  query: URLSearchParams
  browserState: string | null | undefined

interface OAuthProfile
  subject: string
  email: string | null
  emailVerified: boolean

interface OAuthProviderConfig
  id: string
  clientId: string
  clientSecret: string
  authorizationEndpoint: string
  tokenEndpoint: string
  userInfoEndpoint: string
  scopes: readonly string[]
  authorizationParams?: Readonly<Record<string, string>>
  profile(body: unknown): OAuthProfile | null

interface OAuthSignIn
  readonly method: string
  authorizationUrl(): Promise<OAuthAuthorization>
  handleCallback(input: OAuthCallbackInput): Promise<OAuthSignInResult>
  disconnect(userId: number, keyId: number): Promise<boolean>

interface OAuthSignInOptions extends ProviderDeps
  provider: OAuthProviderConfig
  redirectUri: string
  flowTtlSeconds?: number
  timeoutMs?: number
  fetch?: (request: Request) => Promise<Response>

interface OAuthSignInResult extends SignInResult
  outcome: OAuthOutcome
  profile: OAuthProfile

type OAuthFailure = "invalid-request" | "invalid-state" | "provider-error" | "token-exchange-failed" | "profile-failed" | "invalid-profile" | "user-deleted"
```

## `@spy4x/server/auth/oauth-google`

Capability: password sign-up and sign-in, mail codes, OAuth (Google). Source: `server/auth/oauth-google.ts`.

```text
const GOOGLE_AUTHORIZATION_ENDPOINT: "https://accounts.google.com/o/oauth2/v2/auth"

const GOOGLE_DEFAULT_SCOPES: readonly string[]

const GOOGLE_TOKEN_ENDPOINT: "https://oauth2.googleapis.com/token"

const GOOGLE_USERINFO_ENDPOINT: "https://openidconnect.googleapis.com/v1/userinfo"

function createGoogleOAuthProvider(options: GoogleOAuthOptions): OAuthProviderConfig

function readGoogleProfile(body: unknown): OAuthProfile | null

interface GoogleOAuthOptions
  clientId: string
  clientSecret: string
  scopes?: readonly string[]
  authorizationParams?: Readonly<Record<string, string>>
```

## `@spy4x/server/request-log`

Capability: request logging. Source: `server/request-log/log.ts`.

```text
function requestLog<E extends Env = Env>(options: RequestLogOptions): MiddlewareHandler<E>

interface RequestLogOptions
  write?: RequestLogWriter
  skipPaths?: readonly string[]

type RequestLogWriter = (line: string) => void
```

## `@spy4x/server/config`

Capability: typed config from the environment. Source: `server/config/index.ts`.

```text
class ConfigError extends Error
  constructor(public readonly variables: readonly string[])

class MissingEnvError extends Error
  constructor(name: string)

const stringBoolean: Type<(In: "true" | "false") => Out<boolean>>

const systemEnv: EnvReader

function createEnvReader(values: Record<string, string | undefined>): EnvReader

function loadConfig<T extends Type>(schema: T, env: EnvReader): SchemaOutput<T>

function readEnvVar(env: EnvReader, name: string, options: { optional?: boolean; }): string

interface EnvReader
  get(name: string): string | undefined
```

## `@spy4x/server/crypto`

Capability: field encryption. Source: `server/crypto.ts`.

```text
class CryptoError extends Error
  constructor(code: CryptoErrorCode, message: string, options?: { cause?: unknown; })
  readonly code: CryptoErrorCode

class CryptoService implements SecretCipher
  constructor(secret: string)
  static fromHexKey(hex: string): CryptoService
  async encrypt(plaintext: string, context?: string): Promise<string>
  async decrypt(ciphertext: string, context?: string): Promise<string>

const AES_ALGORITHM: "AES-GCM"

const AES_KEY_BYTES: 32

const DEFAULT_MASK_VISIBLE: 4

const HEX_KEY_LENGTH: 64

const IV_LENGTH: 12

const KEY_BITS: 256

const MASK_LENGTH: 12

const MASK_VISIBLE_DIVISOR: 4

const MAX_MASK_VISIBLE: 4

enum CryptoErrorCode
  InvalidSecret = 1
  InvalidHexKey = 2
  MalformedCiphertext = 3
  EncryptionFailed = 4
  DecryptionFailed = 5

function isHexKey(hex: string): boolean

function maskKey(key: string, visible: number): string

interface SecretCipher
  encrypt(plaintext: string, context?: string): Promise<string>
  decrypt(ciphertext: string, context?: string): Promise<string>
```

## `@spy4x/server/db`

Capability: Postgres access, transactions, migrations. Source: `server/db/index.ts`.

```text
async function applySqliteSchema(db: SqliteDb, schema: SqliteSchema): Promise<void>

async function checksumOf(sqlText: string): Promise<string>

async function discoverMigrations(options: DiscoverMigrationsOptions): Promise<string[]>

async function openSqliteDb(options: OpenSqliteDbOptions): Promise<SqliteDb>

async function purgeDatabase(options: PurgeOptions): Promise<PurgeResult>

async function removeSqliteFiles(path: string, remove: (file: string) => Promise<void>): Promise<string[]>

class DbServiceBase
  constructor(options: DbServiceBaseOptions)
  protected sql: Sql
  protected setSql(sql: Sql): void
  async isConnected(): Promise<boolean>
  async begin(fn: (tx: this) => Promise<T>): Promise<T>
  async connect(): Promise<void>
  async shutdown(): Promise<void>
  sanitize(obj: Partial<T>): Partial<T>
  async findOne(cache: RowCache<T>, id: string | number, command: postgres.PendingQuery<T[]>): Promise<null | T>
  async createOne(cache: RowCache<T>, command: postgres.PendingQuery<T[]>): Promise<T>
  async updateOne(cache: RowCache<T>, command: postgres.PendingQuery<T[]>): Promise<T | undefined>
  async deleteOne(cache: RowCache<T>, command: postgres.PendingQuery<T[]>): Promise<T | undefined>
  buildMethods(table: string, cache: RowCache<M>): RowMethods<M, C, U>
  protected setCache(cache: RowCache<T>, id: string | number, value: T): Promise<void>
  protected deleteCache(cache: RowCache<T>, id: string | number): Promise<void>

class MigrationEditedError extends Error
  constructor(migration: string, recordedChecksum: string, fileChecksum: string)
  readonly migration: string
  readonly recordedChecksum: string
  readonly fileChecksum: string

class MigrationRenamedError extends Error
  constructor(recordedName: string, pendingName: string, checksum: string)
  readonly recordedName: string
  readonly pendingName: string
  readonly checksum: string

class PostgresIdentifierTransformError extends Error
  constructor(kind: "table" | "schema", identifier: string, rewrittenTo: string)
  readonly kind: "table" | "schema"
  readonly identifier: string
  readonly rewrittenTo: string

class PostgresMigrationDriver implements MigrationDriver
  constructor(options: PostgresMigrationDriverOptions)
  async withLock(run: () => Promise<T>): Promise<T>
  async createHistoryTable(): Promise<void>
  async appliedMigrations(): Promise<AppliedMigration[]>
  async applyInTransaction(migration: Migration): Promise<void>
  async applyWithoutTransaction(migration: Migration): Promise<void>

class PostgresMigrationLockError extends Error
  constructor(lockWaitMs: number)
  readonly lockWaitMs: number

class PostgresMigrationRunInProgressError extends Error
  constructor()

class PostgresScopeEndedError extends Error
  constructor()

class PostgresUnexpectedRowError extends Error
  constructor(context: string, expected: string, got: unknown)

class SqliteDb
  constructor(driver: SqliteDriver, path: string, transactions, options: SqliteDbOptions)
  get databasePath(): string
  get isOpen(): boolean
  async exec(sql: string): Promise<void>
  async prepare(sql: string): Promise<SqliteStatement>
  async queryOne(sql: string, ...parameters: unknown[]): Promise<T | undefined>
  async queryAll(sql: string, ...parameters: unknown[]): Promise<T[]>
  async execute(sql: string, ...parameters: unknown[]): Promise<SqliteRunResult>
  async pragma(name: string): Promise<unknown>
  async foreignKeysEnabled(): Promise<boolean>
  async transaction(fn: (db: SqliteDb) => Promise<T>): Promise<T>
  async close(): Promise<void>

class SqliteMigrationDriver implements MigrationDriver
  constructor(options: { db: SqliteDb; table?: string; })
  async withLock(run: () => Promise<T>): Promise<T>
  async createHistoryTable(): Promise<void>
  async appliedMigrations(): Promise<AppliedMigration[]>
  async applyInTransaction(migration: Migration): Promise<void>
  async applyWithoutTransaction(migration: Migration): Promise<void>

class SqliteScopeEndedError extends Error
  constructor()

class SqliteTransactionWaitError extends Error
  constructor(milliseconds: number)

const DEFAULT_MIGRATION_LOCK_RETRY_MS: 250

const DEFAULT_MIGRATION_LOCK_WAIT_MS: 60000

const DEFAULT_MIGRATIONS_TABLE: "migrations"

const DEFAULT_POOL_OPTIONS: Required<Pick<PoolOptions, "connectTimeout" | "idleTimeout" | "maxLifetimeSeconds" | "max">>

const DEFAULT_SQLITE_FILE: "db.sqlite"

const DEFAULT_TRANSACTION_STATEMENTS: TransactionStatements

const DEFAULT_TRANSACTION_WAIT_MS: 5000

const ENV_NAME: "ENV"

const MEMORY_PATH: ":memory:"

const NO_TRANSACTION_SUFFIX: ".no_transaction"

const PROD_FLAG: "--prod"

const SAFE_ENV_VALUES: readonly string[]

enum PostgresEnvName
  Host = "DB_HOST"
  Port = "DB_PORT"
  User = "DB_USER"
  Pass = "DB_PASS"
  Name = "DB_NAME"

enum SqliteEnvName
  Path = "DB_SQLITE_PATH"

function buildPostgresOptions(options: CreateSqlOptions): postgres.Options<Record<string, postgres.PostgresType>>

function createSql(options: CreateSqlOptions): Sql

function createSqlFromEnv(environment: Record<string, string | undefined>, options: PoolOptions): Sql | undefined

function parseMigrationName(fileName: string, extension: string): Migration["name"]

function parsePostgresEnv(environment: Record<string, string | undefined>): PostgresConnectionConfig | undefined

function resolveSqlitePath(options: { path?: string; environment?: Record<string, string | undefined>; root?: string; }): string

function runMigrations(driver: MigrationDriver, options: DiscoverMigrationsOptions): Promise<MigrationReport>

interface AppliedMigration
  name: string
  checksum: string | null

interface CreateSqlOptions extends PoolOptions
  connection: PostgresConnectionConfig

interface DbServiceBaseOptions
  sql: Sql

interface DiscoverMigrationsOptions
  folder: string
  extension?: string
  reader?: MigrationReader

interface FindOneParams
  id: string | number
  includeDeleted?: boolean

interface Migration
  fileName: string
  name: string
  sqlText: string
  checksum: string
  withoutTransaction: boolean

interface MigrationDriver
  withLock(run: () => Promise<T>): Promise<T>
  createHistoryTable(): Promise<void>
  appliedMigrations(): Promise<AppliedMigration[]>
  applyInTransaction(migration: Migration): Promise<void>
  applyWithoutTransaction(migration: Migration): Promise<void>

interface MigrationReport
  applied: string[]
  skipped: string[]
  missing: string[]

interface OpenSqliteDbOptions
  createDriver: SqliteDriverFactory
  path?: string
  environment?: Record<string, string | undefined>
  root?: string
  createDirectory?: (directory: string) => void
  foreignKeys?: boolean
  writeAheadLog?: boolean
  transactionWaitMs?: number
  delay?: SqliteDelay

interface PoolOptions
  connectTimeout?: number
  idleTimeout?: number
  maxLifetimeSeconds?: number
  max?: number
  applicationName?: string
  transform?: typeof postgres.camel
  ssl?: postgres.Options<Record<string, postgres.PostgresType>>["ssl"]

interface PostgresConnectionConfig
  host: string
  port?: number
  user: string
  password: string
  database: string

interface PostgresMigrationDriverOptions
  sql: Sql
  table?: string
  lockWaitMs?: number
  lockRetryMs?: number
  delay?: MigrationDelay
  schema?: string

interface PurgeOptions
  sql: Sql
  environment?: Record<string, string | undefined>
  args?: readonly string[]
  schema?: string

interface PurgeResult
  dropped: string[]
  refused: boolean

interface RowCache<T>
  wrap(key: string | number, compute: () => Promise<T>): Promise<T>
  set(key: string | number, value: T): Promise<void>
  delete(key: string | number): Promise<void>

interface RowMethods<M extends postgres.Row, C extends Partial<unknown>, U extends Partial<unknown>>
  findOne(params: FindOneParams): Promise<null | M>
  findChanged(updatedAtGt: Date): Promise<M[]>
  createOne(params: { data: C; }): Promise<M>
  updateOne(params: { id: string | number; data: U; }): Promise<M | undefined>
  deleteOne(params: { id: string | number; }): Promise<M | undefined>
  undeleteOne(params: { id: string | number; }): Promise<M | undefined>

interface SqliteDbOptions
  transactionWaitMs?: number
  delay?: SqliteDelay

interface SqliteDriver
  exec(sql: string): unknown
  prepare(sql: string): SqliteStatement | Promise<SqliteStatement>
  close(): unknown

interface SqliteOpenOptions
  path: string

interface SqliteRunResult
  changes: number | bigint
  lastInsertRowid: number | bigint

interface SqliteSchema
  table: string
  sql: string
  upgrade?: (db: SqliteDb) => Promise<void>

interface SqliteStatement
  get(...parameters: unknown[]): unknown
  all(...parameters: unknown[]): unknown[] | Promise<unknown[]>
  run(...parameters: unknown[]): SqliteRunResult | Promise<SqliteRunResult>

interface TransactionStatements
  begin: string
  commit: string
  rollback: string

type MigrationDelay = (milliseconds: number) => Promise<void>

type Sql = postgres.Sql<Record<string, unknown>>

type SqliteDelay = (milliseconds: number) => { expired: Promise<void>; cancel: () => void; }

type SqliteDriverFactory = (options: SqliteOpenOptions) => SqliteDriver | Promise<SqliteDriver>

type Transaction = postgres.TransactionSql<Record<string, unknown>>
```

## `@spy4x/server/db/migrate`

Capability: Postgres access, transactions, migrations. Source: `server/db/migrate.ts`.

```text
async function checksumOf(sqlText: string): Promise<string>

async function discoverMigrations(options: DiscoverMigrationsOptions): Promise<string[]>

class MigrationEditedError extends Error
  constructor(migration: string, recordedChecksum: string, fileChecksum: string)
  readonly migration: string
  readonly recordedChecksum: string
  readonly fileChecksum: string

class MigrationRenamedError extends Error
  constructor(recordedName: string, pendingName: string, checksum: string)
  readonly recordedName: string
  readonly pendingName: string
  readonly checksum: string

const denoMigrationReader: MigrationReader

const NO_TRANSACTION_SUFFIX: ".no_transaction"

function parseMigrationName(fileName: string, extension: string): Migration["name"]

function runMigrations(driver: MigrationDriver, options: DiscoverMigrationsOptions): Promise<MigrationReport>

interface AppliedMigration
  name: string
  checksum: string | null

interface DiscoverMigrationsOptions
  folder: string
  extension?: string
  reader?: MigrationReader

interface Migration
  fileName: string
  name: string
  sqlText: string
  checksum: string
  withoutTransaction: boolean

interface MigrationDriver
  withLock(run: () => Promise<T>): Promise<T>
  createHistoryTable(): Promise<void>
  appliedMigrations(): Promise<AppliedMigration[]>
  applyInTransaction(migration: Migration): Promise<void>
  applyWithoutTransaction(migration: Migration): Promise<void>

interface MigrationReader
  list(folder: string): Promise<string[]>
  readText(folder: string, fileName: string): Promise<string>

interface MigrationReport
  applied: string[]
  skipped: string[]
  missing: string[]
```

## `@spy4x/server/db/postgres`

Capability: Postgres access, transactions, migrations. Source: `server/db/postgres.ts`.

```text
const DEFAULT_POOL_OPTIONS: Required<Pick<PoolOptions, "connectTimeout" | "idleTimeout" | "maxLifetimeSeconds" | "max">>

enum PostgresEnvName
  Host = "DB_HOST"
  Port = "DB_PORT"
  User = "DB_USER"
  Pass = "DB_PASS"
  Name = "DB_NAME"

function buildPostgresOptions(options: CreateSqlOptions): postgres.Options<Record<string, postgres.PostgresType>>

function createSql(options: CreateSqlOptions): Sql

function createSqlFromEnv(environment: Record<string, string | undefined>, options: PoolOptions): Sql | undefined

function parsePostgresEnv(environment: Record<string, string | undefined>): PostgresConnectionConfig | undefined

interface CreateSqlOptions extends PoolOptions
  connection: PostgresConnectionConfig

interface PoolOptions
  connectTimeout?: number
  idleTimeout?: number
  maxLifetimeSeconds?: number
  max?: number
  applicationName?: string
  transform?: typeof postgres.camel
  ssl?: postgres.Options<Record<string, postgres.PostgresType>>["ssl"]

interface PostgresConnectionConfig
  host: string
  port?: number
  user: string
  password: string
  database: string
```
